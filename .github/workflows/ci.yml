name: CI

on: [push]

jobs:

    lint:
            runs-on: ubuntu-latest

            steps:
                   - name: Runs linter
                    run: npm run lint


    build:
            runs-on: ubuntu-latest
            steps:
                   - name: Runs build
                    run: npm run build

